# 聚合模式与边界

## 适用模式

- count / sum / avg / min / max
- 分组统计
- top n / 排名
- 核心目标是聚合结果，而不是返回对象明细列表

## 常见线索

- “统计有多少”
- “按 region 分组统计”
- “按金额求和并排序”
- “排名前 10”

## 示例

### 正例

用户：按 region 统计订单数量，并按数量降序

应生成：`AGGREGATE`

### 反例

用户：列出金额大于 1000 的订单

这是普通读取，不应生成聚合。
