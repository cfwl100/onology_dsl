# 重构说明

本次修订针对上一版继续收敛：

1. 顶层入口改为纯意图层表述，不再出现内部语法名词或实现术语。
2. 各目录 `SKILL.md` 去掉对外部说明书的依赖，改为引用本目录 `references/`。
3. 各目录 `SKILL.md` 不再使用额外语法名词做教学说明，只保留当前操作需要的边界。
4. `references/` 中删除与当前目录无关的结构说明，例如普通读取不再包含写入块与分组指标写法。
5. 所有目录补充边界样例，覆盖最小有效结构、函数表达式、逻辑组、排序、子查询、复合写入等场景。
6. `tests/` 新增对全部 Python 脚本的测试，既覆盖公共脚本，也覆盖各目录包装脚本。
