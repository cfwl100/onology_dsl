# 存在则更新 / 批量写入操作符参考

## 存在则更新字段
- `matchBy`：匹配键数组
- `data`：写入数据对象

## 批量写入字段
- `atomic`：布尔值，表示是否原子提交
- `items`：子请求数组

## 约束
- `matchBy` 中的每个字段都必须出现在 `data` 中。
- `items` 中每个子项都必须独立合法。
- 子项不允许再次使用 `BATCH`。
