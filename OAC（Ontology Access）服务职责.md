## 1.6 OAC（Ontology Access）服务职责

OAC 是 OQL 的执行入口与转换引擎，其核心职责包括：

### 1）元数据绑定

将 OQL 中的：

- `objectType`
- `field`
- `relationshipType`

绑定到：

- 本体对象模型
- 属性映射元数据
- 关系映射元数据
- 物理数据源信息

### 2）逻辑执行计划生成

根据 OQL 生成逻辑计划，例如：

- 单对象单源查询
- 单对象多源属性拼装
- 多对象同库 JOIN
- 同表多对象自连接
- 先子查询后聚合
- 先图库遍历后关系库回查

### 3）物理查询翻译

将逻辑计划翻译为：

- MySQL SQL
- PostgreSQL SQL
- Gauss SQL
- ClickHouse SQL
- NebulaGraph nGQL
- 其他物理访问语句

### 4）多源执行编排

当无法下推为单条物理语句时，OAC 负责：

- 子计划拆分
- 并行执行
- 中间结果集管理
- 回表查询
- 跨源 Join / Union / Intersection
- 结果去重、排序、聚合与装配

### 5）结果还原

将物理结果还原为 OQL 所定义的逻辑结果结构，并保留：

- alias 语义
- 字段命名
- 元数据
- 错误与 trace 信息

------

## 1.7 典型物理映射场景

### 1.7.1 单对象，单源属性

所有属性来自同一数据库、同一表。
OAC 可直接生成一条 SQL 或 nGQL。

### 1.7.2 单对象，多源属性

同一对象的属性分布在多个数据源。
OAC 需：

1. 选择主源或 identity source
2. 下推可下推条件
3. 获取对象 id 集合
4. 到其他源按 id 回查
5. 在中间层装配结果

### 1.7.3 多对象，同库不同表

多个对象映射到同一数据库的不同表。
若映射中存在明确连接键，OAC 可直接生成 JOIN SQL。

### 1.7.4 多对象，同库同表

多个对象映射到同一张物理表。
此时需区分：

- 是否只是同一实例的重复投影
- 是否代表不同角色
- 是否需要对象间关系约束
- 是否需要同时返回两个实例

若是不同角色或存在对象间约束，则必须生成**多别名访问或自连接**。

### 1.7.5 关系在图数据库，属性在关系库

例如：

- 关系：NebulaGraph
- 属性：MySQL / PostgreSQL

OAC 通常采用：

1. 图查询先取对象 id 或路径
2. 关系库按 id 回查属性
3. 中间层装配

### 1.7.6 使用 `sourceQuery` 的中间结果编排

当外层查询依赖子查询结果时，OAC 可将 `sourceQuery` 视为逻辑中间结果集，而不要求其必须是一条物理 SQL 子查询。

------

## 1.8 查询下推与组合原则

OAC 在执行 OQL 时应优先遵循以下原则：

### 1）优先单源下推

只要过滤、排序、聚合可在同一物理源完成，应优先下推，减少中间层数据量。

### 2）跨源只做必要合并

跨源场景应尽量：

- 先缩小 id 集合
- 再回查补充字段
- 避免跨源全量宽表拼装

### 3）过滤优先于字段补齐

若某些条件只能在主源下推，应先筛掉大部分对象，再去其他源补字段。

### 4）聚合尽量下推

若聚合字段与分组字段位于同一物理源，应优先在该源侧完成 `GROUP BY / SUM / COUNT`。

### 5）图遍历优先在图库完成

只要关系路径存在于 NebulaGraph 等图数据库，应优先在图库完成路径查找，再回查属性。

------

## 1.9 跨数据源支持边界

为保证实现可控性，建议 OQL/OAC 的跨源能力边界定义如下：

### 支持

- 单对象跨源属性拼装
- 同库 JOIN 下推
- 同表多角色自连接
- 图关系 + 关系属性的组合查询
- 过滤下推
- 聚合下推
- `sourceQuery` 驱动的中间结果集编排

### 限制

- 超大结果集的跨源全量 JOIN
- 不同源之间高成本全表比较
- 分布式强一致事务跨源写入
- 不同源时区、精度、编码不兼容字段的直接比较
- 缺少明确 identity key 时的跨源对象合并

### 建议

- 优先单源求值
- 跨源只做必要字段补齐
- 对无法下推或性能退化的请求，在响应 metadata 中标记执行退化信息
- 对高风险写操作要求显式主写源与唯一匹配条件

------

## 1.10 面向实现的总体建议

OQL 与 OAC 的推荐实现方式是：

> **OQL → AST → 本体绑定 → 逻辑计划 → 物理子计划 → 多源执行编排 → 结果装配**

而不是：

> **OQL → 直接拼接 SQL / nGQL**

推荐开发时显式实现以下模块：

1. **Parser / AST Builder**
2. **Ontology Binder**
3. **Source Grouper**
4. **Predicate Splitter**
5. **Logical Planner**
6. **Dialect Generator**
7. **Executor / Orchestrator**
8. **Result Assembler**