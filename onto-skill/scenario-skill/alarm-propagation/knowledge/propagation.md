# propagation 知识片段

本文供外层 `SKILL.md` 摘录并组装 6 行 Planning 输入。

## 本体ID

```text
network@1.0
```

## 业务意图

```text
获取 ${target} 相关的业务关系知识，用于辅助分析。
```

## 业务领域知识

### 场景知识

填写本场景的业务关系含义、适用范围和边界。

### 检索规则

填写需要关注的对象类型、关系类型和返回结构要求。

### 规划规则

填写从子图到任务规划的业务约束。

### 查询规则

填写本场景需要的数据范围、字段和筛选要求。

### 返回要求

填写最终需要返回的对象、关系和摘要说明。

### 失败策略

填写缺少目标、缺少规则或结果为空时的处理方式。

## 流程级定制

```text
使用默认流程。
```

## 步骤级定制

```text
使用默认步骤模板；业务增量规则见业务领域知识。
```

## 缺失信息

```text
如果缺少 ${target} 或必要业务规则，填写对应缺失项；否则填写无。
```

## 可注入 6 行片段

```text
本体ID：network@1.0
业务意图：获取 ${target} 相关的业务关系知识，用于辅助分析。
业务领域知识：填写本场景的业务关系含义、适用范围、对象类型、关系类型、查询范围、返回结构和失败策略。
流程级定制：使用默认流程。
步骤级定制：使用默认步骤模板；业务增量规则见业务领域知识。
缺失信息：如果缺少 ${target} 或必要业务规则，填写对应缺失项；否则填写无。
```
