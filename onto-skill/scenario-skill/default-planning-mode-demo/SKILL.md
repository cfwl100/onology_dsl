---
name: default-planning-mode-demo
description: 默认规划模式业务 Skill 示例。业务侧只提供公共本体ID、业务意图和少量业务领域知识，由 Ontology-based-planning-skill 按当前 S1-S6 默认流程规划。
allowed_tools:
metadata:
  mode: default_planning_demo
  planning_protocol: six-line-business-domain-knowledge
  planning_steps: S1-S6
---

# 默认规划模式业务 Skill 示例

## 1. 使用场景

当业务侧只知道用户目标，但没有定制 SOP 或完整步骤时，使用本示例模式。

典型请求：

- 查询船舶类型为货轮且船高大于 10 的船舶信息。
- 查询船舶及其船舶计划。
- 找到和船舶相关的对象、字段和关系，并完成默认规划。

## 2. 职责边界

你是上层业务 Skill 示例，只负责：

1. 识别用户目标。
2. 将用户问题改写成详细自然语言业务意图。
3. 提供公共 `本体ID`。
4. 提供必要的业务领域知识；没有则写无。
5. 委托 `Ontology-based-planning-skill`。

你不直接调用平台能力，不提前臆造对象、字段或关系。

## 3. 当前 Planning 协议

Planning 层当前使用 6 行顶层输入：

```text
本体ID：<公共本体ID>
业务意图：<详细自然语言问题>
业务领域知识：<场景知识、规则来源、子图检索规则、任务规划规则、查询规则、返回要求、Function 规则和失败策略；没有则写无>
流程级定制：<只填写相对默认流程的覆盖；无覆盖写“使用默认流程”>
步骤级定制：<只填写相对默认步骤模板的业务增量规则；无增量写“使用默认步骤模板”>
缺失信息：<没有则写无>
```

## 4. 输入示例

当用户请求“查询船高大于 10 的货轮”时，构造如下自然语言输入并委托给 Planning 层：

```text
本体ID：dtmi.ontology.560d88f7.1
业务意图：查询船舶信息，过滤条件为船高大于 10 且船舶类型为货轮；希望返回船舶类型和船高，maxResults 为 1000；请先检索相关本体子图，再基于子图确认对象、字段归属和是否需要数据访问。
业务领域知识：无
流程级定制：使用默认流程 S1 -> S2 -> S3 -> S6；不执行 S4/S5 Function。
步骤级定制：使用默认步骤模板；业务增量规则见业务意图。
缺失信息：无
```

## 5. 当前步骤编号

Planning 层默认流程：

```text
S1 子图检索 -> S2 基于本体子图的任务规划 -> S3 OAC 查询 -> S6 汇总
```

如果用户只要求解释模型结构或规划结果，可由 Planning 层在 S2 后进入 S6 汇总。

## 6. 禁止事项

- 禁止跳过本体子图检索直接拼接平台请求。
- 禁止把 `has_property` 当成对象间关系。
- 禁止把未通过 `has_property` 确认归属的字段写入对象。
- 禁止自行臆造关系名、字段名或函数参数。
- 禁止要求业务侧同时填写 ontologyId 和 schemaRef。
